{
  "name": "callback",
  "version": "0.0.1",
  "main": "dist/index.js",
  "description": "Little TypeScript library of callback functions, mostly for array methods like map/filter/reduce and sort.",
  "repository": "git@github.com:kevinmaes/callback",
  "author": "Kevin Maes <kevin@kevinmaes.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && yarn tsup src/index.ts --format esm,cjs"
  },
  "dependencies": {
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@preconstruct/cli": "^2.1.5",
    "tsup": "^6.1.2"
  }
}
